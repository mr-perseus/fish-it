<?xml version="1.0" encoding="utf-8"?>

<ContentPage NavigationPage.HasBackButton="False"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Fishit.Presentation.UI.MarkupExtensions"
             x:Class="Fishit.Presentation.UI.Views.AuthPage">
    <ContentPage.Content>
        <StackLayout>
            <Image VerticalOptions="CenterAndExpand"
                   Source="{local:EmbeddedImage Fishit.Presentation.UI.Images.logo.png}" />
            <ActivityIndicator x:Name="LoadingIndicator" Color="{StaticResource PrimaryColor}" IsRunning="False"></ActivityIndicator>
            <Button VerticalOptions="EndAndExpand"
                    Text="Login"
                    Clicked="Login_OnClicked"
                    Style="{StaticResource ButtonStyle}"
                    Margin="20,0" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>